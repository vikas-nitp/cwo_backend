# DEAD CODE — re-export never consumed via this __init__; all callers import directly from domain.models
# Remove in next cleanup pass (2026-09-20)
# from app.domain.models import Offer
# __all__ = ["Offer"]
